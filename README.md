
import numpy as np
from numpy import random
price=np.random.normal(73000,72000,10)
print("simulate :", price)
print("mean :" ,np.mean(price),"max value :",np.max(price),"min value :",np.min(price))

